program condicional;
begin
end


{var
    x, y: integer;
begin
    'aloha' ahhd
    read(x);
    read(y);
    if (x >= 10) then
    begin
        write(x);
    end
    else
    begin
        write(y);
    end;
end.}